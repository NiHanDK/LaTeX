\definecolor{SQLMSSQLIdentifierstyle}{HTML}{008080}
\definecolor{SQLMSSQLComment}{HTML}{008000}
\definecolor{SQLMSSQLString}{HTML}{FF0000}
\definecolor{SQLMSSQLKeyword}{HTML}{0000FF}

\lstdefinelanguage[MSSQL]{SQL}[]{SQL} {
	morekeywords={
		@@FETCH_STATUS,
		AS,
		BEGIN,
		CASCADE,
		CLOSE,
		CLUSTERED,
		CURSOR,
		DATABASE,
		DECLARE,
		FETCH,
		FOR,
		GO,
		IF,
		INSTEAD,
		ISNULL,
		OF,
		OPEN,
		PROCEDURE, 
		REFERENCES,
		USE,
		WHILE,
	}
}

\lstdefinestyle{MSSQL}{
	language = [MSSQL]{SQL},
	style = defaultLayout,
    identifierstyle=\color{SQLMSSQLIdentifierstyle},
	keywordstyle=\color{SQLMSSQLKeyword},          		% Keyword style
	commentstyle=\color{SQLMSSQLComment},       		% Comment style
	stringstyle=\color{SQLMSSQLString},         		% String literal style
	escapeinside={\%*}{*)}            					% LaTeX inside listing
}